Important steps for the project:

To bringe the changes of SASS to app.css. Run below mentioned command:
Below command keep the track of the changes from scss to app.css
Run below mentioned command:


# npm - need to install if not present.
# install sass as well: npm install npm-sass
# run the package.json: npm install package.json
# compile-scss   -> run script description from the project.

$ npm run compile-sass

Website Status: css directly put in css file not from scss. 
if you rub scss then thex get convereted to orange.
Work on the scss file colours is the first task in the project. 